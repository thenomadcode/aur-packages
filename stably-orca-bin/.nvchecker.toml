[stably-orca-bin]
source = "github"
github = "stablyai/orca"
use_max_tag = true
prefix = "v"
# Stable releases only. Skip -rc.N / -alpha / -beta / etc.
# nvchecker applies include_regex before prefix is stripped, so keep the v.
include_regex = '^v\d+\.\d+\.\d+$'
