[stably-orca-bin]
source = "github"
github = "stablyai/orca"
# Use /releases/latest — excludes drafts, pre-releases, and tags that never
# shipped assets (e.g. v1.3.0 was tagged upstream but had no AppImage).
use_latest_release = true
prefix = "v"
